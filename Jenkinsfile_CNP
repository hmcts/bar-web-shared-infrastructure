#!groovy
//noinspection GroovyUnusedAssignment
@Library("Infrastructure") _

import uk.gov.hmcts.contino.Environment

Environment environment = new Environment(env)

onMaster {
  sharedInfrastructurePipeline('bar-web', environment.nonProdName, 'nonprod')
  sharedInfrastructurePipeline('bar-web', environment.prodName, 'prod')
}

onDemo {
  sharedInfrastructurePipeline('bar-web', environment.demoName, 'nonprod')
}
