#!groovy
//noinspection GroovyUnusedAssignment
@Library("Infrastructure") _

import uk.gov.hmcts.contino.Environment

Environment environment = new Environment(env)

onMaster {
  sharedInfrastructurePipeline('bar', environment.nonProdName, 'nonprod')
  sharedInfrastructurePipeline('bar', environment.demoName, 'nonprod')
  sharedInfrastructurePipeline('bar', environment.prodName, 'prod')
}
